import styles from './navbar.module.scss'

const Navbar = () => {
  return (
    <nav className={`${styles.nav} default-format`}>
      <div>1</div>
      <div>2</div>
      <div>3</div>
    </nav>
  )
}

export default Navbar